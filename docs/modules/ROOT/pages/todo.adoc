= Operator ToDo
:page-title: ToDo

Priority backlog based on the current repository state.

== Done

- [x] URL safety policy
* [x] allowlist and blocklist controls for outbound action URLs
* [x] localhost and metadata endpoints blocked by default
* [x] runtime enforcement in the HTTP executor
* [x] unit tests for URL policy behavior
* [x] spec validation plus `SpecValid` condition

- [x] Admission webhook deployment foundation
* [x] validating webhook implementation exists
* [x] webhook registration in the manager exists
* [x] Helm deployment wiring for webhook service, cert mount, and `ValidatingWebhookConfiguration` exists
* [x] cert-manager integration for webhook certificates exists

== P1 Next

- [ ] Admission validation webhook hardening
* [x] reject invalid `ResourceAction` specs before runtime
* [x] validate mode, schedule, URL, regex, env, and volume structure
* [ ] validate referenced Secrets for TLS and header/env secret refs more strictly
* [ ] add end-to-end webhook validation coverage

- [ ] Correct `once` semantics at action level
* [ ] dedupe by `uid + event + action`
* [ ] avoid skipping all actions when only one action already executed

- [ ] Retry and circuit breaker controls
* [x] basic HTTP retry policy exists
* [ ] cap repeated failures per target
* [ ] cooldown windows and max in-flight per action
* [ ] circuit-breaker style suppression after repeated failures

== P2 Important

- [ ] Multi-tenant hardening
* [ ] stricter secret and namespace boundaries
* [ ] optional per-namespace quotas

- [ ] Better status model
* [ ] per-action summaries instead of only aggregate execution records
* [ ] clearer failure classification
* [ ] more explicit separation of HTTP and Job execution summaries

- [ ] Dead-letter or fallback hooks
* [ ] on terminal failure, emit to fallback endpoint, topic, or event target

- [ ] E2E conformance matrix
* [x] basic manager and metrics smoke tests exist
* [ ] deterministic tests for create, update, and delete execution
* [ ] deterministic tests for TLS and mTLS
* [ ] deterministic tests for retries and failure behavior
* [ ] deterministic tests for webhook validation
