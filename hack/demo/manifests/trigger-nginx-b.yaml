apiVersion: apps/v1
kind: Deployment
metadata:
  name: trigger-nginx-b
  namespace: ra-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trigger-nginx-b
  template:
    metadata:
      labels:
        app: trigger-nginx-b
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
