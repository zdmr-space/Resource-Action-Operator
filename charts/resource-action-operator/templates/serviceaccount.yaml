{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rao.serviceAccountName" . }}
  labels:
    {{- include "rao.labels" . | nindent 4 }}
{{- end }}
